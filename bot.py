Отлично, это и есть ваш личный Telegram ID.## Что делать дальше

### 1. Скачайте новый `bot.py` и замените им файл на GitHub
(Upload files → выберите новый → Commit changes, как делали раньше)

### 2. Добавьте новую переменную на Railway
Зайдите в **Variables** вашего сервиса и добавьте:

| Переменная | Значение |
|---|---|
| `ADMIN_ID` | `8704649787` |

(остальные переменные `TELEGRAM_TOKEN`, `GROQ_API_KEY`, `GROUP_ID`, `DB_PATH` оставляем как есть)

### 3. Подтвердите Deploy
После добавления переменной Railway покажет "Apply changes" — нажмите **Deploy**

---

## Как теперь пользоваться ботом

**Важно:** старая база сотрудников (где вы — `Vladislav`) использовала другую структуру таблицы (`name` вместо `last_name`/`first_name`). Новый код ожидает новую структуру, поэтому **старые записи не подойдут** — вас как сотрудника тоже придётся добавить заново через новую систему.

**Тест:**
1. После деплоя отправьте боту видео с любого аккаунта (например, со своего рабочего, не админского)
2. Бот скажет "вы не зарегистрированы", а вам как админу придёт уведомление с этим видео
3. **Ответьте (Reply)** на это уведомление командой `/add_worker`
4. Бот спросит фамилию → имя → должность → группу (можно `0` для группы по умолчанию)
5. После этого попробуйте отправить новое видео с того же аккаунта — теперь должно уйти в группу с новым форматом

Сделайте деплой и попробуйте — дайте знать, что получится на каждом шаге.
